好不容易把GPU中的代码给调通。。。

模式就是两个矩阵的相乘，因为在cl文件里不支持0b数组的写法，当然也不可能支持bitset直接的操作。。

所以采用的最原始的 矩阵相乘代码

只测了编码的部分，跑了1000数据，大概在GPU中 跑了29.0632ms，在CPU中跑，只需要2ms，真的是差距非常大。。。（平台是我自己的平台。。。）

不知道毕设该是什么样子。。。

写OpenCL代码，可以设置全局id，本地id，然后设置size什么的，可以优化一部分的代码。。要好好学习NDRang那部分的知识。。。
