OpenMP训练 就到这里，

只加速了写的状态，原来写1GB的数据大概是617.95 s，现在开启默认的加速线程后，写数据大概是283s，提高了不少

但是读这一块。。总是出错，原因是for循环之间的嵌套 ？

我觉得原因应该是。。。在同一个时间，可能会不同的线 程，访问了同一个数组元素，导致程序崩溃的。。

Read就暂时不用并行了吧。  。现 在的记录差不多是508s, 13min，开始学习OpenCL相关的编程基础。。。
